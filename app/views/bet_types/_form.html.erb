<%= form_for(@bet_type) do |f| %>
  <% if @bet_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bet_type.errors.count, "error") %> prohibited this bet_type from being saved:</h2>

      <ul>
      <% @bet_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
